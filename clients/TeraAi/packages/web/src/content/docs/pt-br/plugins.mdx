---
title: Plugins
description: Escreva seus prأ³prios plugins para estender o opencode.
---

Plugins permitem que vocأھ estenda o opencode conectando-se a vأ،rios eventos e personalizando o comportamento. Vocأھ pode criar plugins para adicionar novos recursos, integrar-se a serviأ§os externos ou modificar o comportamento padrأ£o do opencode.

Para exemplos, confira os [plugins](/docs/ecosystem#plugins) criados pela comunidade.

---

## Usando um plugin

Existem duas maneiras de carregar plugins.

---

### De arquivos locais

Coloque arquivos JavaScript ou TypeScript no diretأ³rio de plugins.

- `.opencode/plugins/` - Plugins em nأ­vel de projeto
- `~/.config/opencode/plugins/` - Plugins globais

Os arquivos nesses diretأ³rios sأ£o carregados automaticamente na inicializaأ§أ£o.

---

### Do npm

Especifique pacotes npm no seu arquivo de configuraأ§أ£o.

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["opencode-helicone-session", "opencode-wakatime", "@my-org/custom-plugin"]
}
```

Pacotes npm regulares e escopados sأ£o suportados.

Navegue pelos plugins disponأ­veis no [ecossistema](/docs/ecosystem#plugins).

---

### Como os plugins sأ£o instalados

**Plugins npm** sأ£o instalados automaticamente usando Bun na inicializaأ§أ£o. Pacotes e suas dependأھncias sأ£o armazenados em cache em `~/.cache/opencode/node_modules/`.

**Plugins locais** sأ£o carregados diretamente do diretأ³rio de plugins. Para usar pacotes externos, vocأھ deve criar um `package.json` dentro do seu diretأ³rio de configuraأ§أ£o (veja [Dependأھncias](#dependencies)), ou publicar o plugin no npm e [adicionأ،-lo أ  sua configuraأ§أ£o](/docs/config#plugins).

---

### Ordem de carregamento

Os plugins sأ£o carregados de todas as fontes e todos os hooks sأ£o executados em sequأھncia. A ordem de carregamento أ©:

1. Configuraأ§أ£o global (`~/.config/opencode/opencode.json`)
2. Configuraأ§أ£o do projeto (`opencode.json`)
3. Diretأ³rio de plugins global (`~/.config/opencode/plugins/`)
4. Diretأ³rio de plugins do projeto (`.opencode/plugins/`)

Pacotes npm duplicados com o mesmo nome e versأ£o sأ£o carregados uma vez. No entanto, um plugin local e um plugin npm com nomes semelhantes sأ£o carregados separadamente.

---

## Criando um plugin

Um plugin أ© um **mأ³dulo JavaScript/TypeScript** que exporta uma ou mais funأ§أµes de plugin. Cada funأ§أ£o recebe um objeto de contexto e retorna um objeto de hooks.

---

### Dependأھncias

Plugins locais e ferramentas personalizadas podem usar pacotes npm externos. Adicione um `package.json` ao seu diretأ³rio de configuraأ§أ£o com as dependأھncias necessأ،rias.

```json title=".opencode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

O opencode executa `bun install` na inicializaأ§أ£o para instalar esses pacotes. Seus plugins e ferramentas podem entأ£o importأ،-los.

```ts title=".opencode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Estrutura bأ،sica

```js title=".opencode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

A funأ§أ£o do plugin recebe:

- `project`: As informaأ§أµes do projeto atual.
- `directory`: O diretأ³rio de trabalho atual.
- `worktree`: O caminho do worktree do git.
- `client`: Um cliente SDK do opencode para interagir com a AI.
- `$`: A [API shell](https://bun.com/docs/runtime/shell) do Bun para executar comandos.

---

### Suporte a TypeScript

Para plugins TypeScript, vocأھ pode importar tipos do pacote de plugin:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@tera-system/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Eventos

Plugins podem se inscrever em eventos como visto abaixo na seأ§أ£o Exemplos. Aqui estأ، uma lista dos diferentes eventos disponأ­veis.

#### Eventos de Comando

- `command.executed`

#### Eventos de Arquivo

- `file.edited`
- `file.watcher.updated`

#### Eventos de Instalaأ§أ£o

- `installation.updated`

#### Eventos LSP

- `lsp.client.diagnostics`
- `lsp.updated`

#### Eventos de Mensagem

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### Eventos de Permissأ£o

- `permission.asked`
- `permission.replied`

#### Eventos de Servidor

- `server.connected`

#### Eventos de Sessأ£o

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Eventos de Todo

- `todo.updated`

#### Eventos de Shell

- `shell.env`

#### Eventos de Ferramenta

- `tool.execute.after`
- `tool.execute.before`

#### Eventos TUI

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Exemplos

Aqui estأ£o alguns exemplos de plugins que vocأھ pode usar para estender o opencode.

---

### Enviando notificaأ§أµes

Envie notificaأ§أµes quando certos eventos ocorrerem:

```js title=".opencode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "opencode"'`
      }
    },
  }
}
```

Estamos usando `osascript` para executar AppleScript no macOS. Aqui estamos usando para enviar notificaأ§أµes.

:::note
Se vocأھ estiver usando o aplicativo desktop opencode, ele pode enviar notificaأ§أµes do sistema automaticamente quando uma resposta estiver pronta ou quando ocorrer um erro na sessأ£o.
:::

---

### Proteأ§أ£o .env

Impeأ§a o opencode de ler arquivos `.env`:

```javascript title=".opencode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Injetando variأ،veis de ambiente

Injete variأ،veis de ambiente em todas as execuأ§أµes de shell (ferramentas de AI e terminais de usuأ،rio):

```javascript title=".opencode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Ferramentas personalizadas

Plugins tambأ©m podem adicionar ferramentas personalizadas ao opencode:

```ts title=".opencode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@tera-system/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

O helper `tool` cria uma ferramenta personalizada que o opencode pode chamar. Ele aceita uma funأ§أ£o de esquema Zod e retorna uma definiأ§أ£o de ferramenta com:

- `description`: O que a ferramenta faz
- `args`: Esquema Zod para os argumentos da ferramenta
- `execute`: Funأ§أ£o que أ© executada quando a ferramenta أ© chamada

Suas ferramentas personalizadas estarأ£o disponأ­veis para o opencode junto com as ferramentas integradas.

:::note
Se uma ferramenta de plugin usar o mesmo nome que uma ferramenta integrada, a ferramenta de plugin tem precedأھncia.
:::

---

### Registro

Use `client.app.log()` em vez de `console.log` para registro estruturado:

```ts title=".opencode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Nأ­veis: `debug`, `info`, `warn`, `error`. Veja a [documentaأ§أ£o do SDK](https://opencode.ai/docs/sdk) para detalhes.

---

### Hooks de compactaأ§أ£o

Personalize o contexto incluأ­do quando uma sessأ£o أ© compactada:

```ts title=".opencode/plugins/compaction.ts"
import type { Plugin } from "@tera-system/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

O hook `experimental.session.compacting` أ© acionado antes que o LLM gere um resumo de continuaأ§أ£o. Use-o para injetar contexto especأ­fico de domأ­nio que o prompt de compactaأ§أ£o padrأ£o poderia perder.

Vocأھ tambأ©m pode substituir o prompt de compactaأ§أ£o completamente definindo `output.prompt`:

```ts title=".opencode/plugins/custom-compaction.ts"
import type { Plugin } from "@tera-system/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

Quando `output.prompt` أ© definido, ele substitui completamente o prompt de compactaأ§أ£o padrأ£o. O array `output.context` أ© ignorado neste caso.
