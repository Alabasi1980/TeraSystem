---
title: Wtyczki
description: Napisz wإ‚asnؤ… wtyczkؤ™, aby rozszerzyؤ‡ opencode.
---

Wtyczki umoإ¼liwiajؤ…ce wyإ‚ؤ…czenie opencode poprzez podjؤ™cie rأ³إ¼nych zdarzeإ„ i zachowanie. Moإ¼na utworzyؤ‡ wtyczkؤ™, aby sprawdziؤ‡ nowe funkcje, integrowaؤ‡ siؤ™ z usإ‚ugami lub sprawdzaniem zachowania opencode.

Aby znaleإ؛ؤ‡ siؤ™ z przykإ‚adami, przejrzyj [wtyczki](/docs/ecosystem#plugins) utworzone przez spoإ‚ecznoإ›ؤ‡.

---

## Uإ¼yj wtyczki

Istniejؤ… dwa sposoby إ‚adowania wtyczek.

---

### Z plikأ³w lokalnych

Miejsce plikأ³w JavaScript lub TypeScript w katalogu wtyczki.

- `.opencode/plugins/` - Wtyczki na poziomie projektu
- `~/.config/opencode/plugins/` - Wtyczki globalne

Pliki w tych katalogach sؤ… automatycznie إ‚adowane podczas uruchamiania.

---

### Z npm

szczegأ³إ‚owy pakiet npm w pliku konfiguracyjnym.

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["opencode-helicone-session", "opencode-wakatime", "@my-org/custom-plugin"]
}
```

Obsإ‚ugiwane sؤ… zarأ³wno pakiety zwykإ‚e, jak i pakiety npm o okreإ›lonym zakresie.

Przeglؤ…daj dostؤ™pnؤ… wtyczkؤ™ w [ekosystemie](/docs/ecosystem#plugins).

---

### Sposأ³b instalacji wtyczek

**wtyczki npm** sؤ… instalowane automatycznie przy uإ¼yciu Bun podczas uruchamiania. Pakiety i ich zaleإ¼noإ›ci sؤ… buforowane w `~/.cache/opencode/node_modules/`.

**Wtyczki lokalne** sؤ… إ‚adowane bezpoإ›rednio z katalogu wtyczek. Aby uzyskaؤ‡ dostؤ™p do zewnؤ™trznych pakietأ³w, musisz zainstalowaؤ‡ `package.json` w swoim katalogu konfiguracyjnym (zobacz [Zaleإ¼noإ›ci](#dependencies)) lub opublikowaؤ‡ wtyczkؤ™ w npm i [dodaj jؤ… do swojej konfiguracji](/docs/config#plugins).

---

### Kolejnoإ›ؤ‡ إ‚adowania

Wtyczki sؤ… إ‚adowane ze wszystkich إ؛rأ³deإ‚, a wszystkie hooki napisane po kolei. Kolejnoإ›ؤ‡ إ‚adowania do:

1. Konfiguracja globalna (`~/.config/opencode/opencode.json`)
2. Konfiguracja projektu (`opencode.json`)
3. Globalny katalog wtyczek (`~/.config/opencode/plugins/`)
4. Katalog wtyczek projektu (`.opencode/plugins/`)

Zduplikowane pakiety npm o tej samej nazwie i wersji sؤ… إ‚adowane raz. Rأ³wnie waإ¼ny jest dostؤ™p lokalny i zewnؤ™trzny npm o dodatkowych nazwach, ktأ³re sؤ… zewnؤ™trzne.

---

## Utwأ³rz wtyczkؤ™

Wtyczka do **moduإ‚ JavaScript/TypeScript**, ktأ³ry eksportuje jeden lub wiؤ™cej wtyczek
funkcje. dostؤ™pna funkcja korzystania z obiektu kontekstu i gإ‚أ³wnego obiektu hooks.

---

### Zaleإ¼noإ›ci

Lokalne narzؤ™dzie i narzؤ™dzie, ktأ³re moإ¼na wykorzystaؤ‡ z zewnؤ™trznych pakietأ³w npm. Dodaj `package.json` do swojego katalogu konfiguracyjnego z zaleإ¼noإ›ciami.

```json title=".opencode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

opencode uruchomienie `bun install` przy uruchomieniu, aby je uruchomiؤ‡. Twoje dodatkowe funkcje bؤ™dؤ… dostؤ™pne po zaimportowaniu.

```ts title=".opencode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Podstawowa struktura

```js title=".opencode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

Funkcja dostؤ™pu do:

- `project`: Aktualne informacje o projekcie.
- `directory`: Bieإ¼ؤ…cy katalog roboczy.
- `worktree`: إڑcieإ¼ka eksploatacyjna Git.
- `client`: Klient SDK z opencode do sztucznej inteligencji.
- `$`: [API obudowy](https://bun.com/docs/runtime/shell) Buna do wykonywania oprogramowania.

---

### Obsإ‚uga TypeScriptu

W przypadku wtyczek TypeScript moإ¼esz importowaؤ‡ typy z pakietu wtyczek:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@tera-system/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Wydarzenia

Wtyczki mogؤ… subskrybowaؤ‡ zdarzenia, jak zastosowaؤ‡ poniإ¼ej sekcji Przykإ‚ady. Oto lista rأ³إ¼nych dziaإ‚aإ„.

#### Wydarzenia dowodzenia

- `command.executed`

#### Plik Wydarzenia

- `file.edited`
- `file.watcher.updated`

#### Wydarzenia instalacyjne

- `installation.updated`

#### Wydarzenia LSP

- `lsp.client.diagnostics`
- `lsp.updated`

#### Wydarzenia w wiadomoإ›ciach

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### Wydarzenia zwiؤ…zane z uprawnieniami

- `permission.asked`
- `permission.replied`

#### Wydarzenia serwerowe

- `server.connected`

#### Wydarzenia sesyjne

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Wydarzenia do zrobienia

- `todo.updated`

#### Wydarzenia shell

- `shell.env`

#### Wydarzenia narzؤ™dziowe

- `tool.execute.after`
- `tool.execute.before`

#### Wydarzenia TUI

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Przykإ‚ady

Oto kilka dostؤ™pnych wtyczek, ktأ³re moإ¼na udostؤ™pniؤ‡ do opencode.

---

### Wysyإ‚aj powiadomienia

Wysyإ‚aj powiadomienia, gdy wystؤ…piؤ… okreإ›lone zdarzenia:

```js title=".opencode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "opencode"'`
      }
    },
  }
}
```

Uإ¼ywamy `osascript` do uruchamiania AppleScript na macOS. Tutaj moإ¼esz przejإ›ؤ‡ do wysyإ‚ania pozostaإ‚oإ›ci.

:::note
Jeإ›li korzystasz z aplikacji komputerowej opencode, moإ¼e ona automatycznie wysإ‚aؤ‡ wiadomoإ›ؤ‡ systemowؤ…, gdy odpowiedإ؛ jest gotowa lub gdy wystؤ…piؤ… bإ‚ؤ™dy sesji.
:::

---

### ochrona .env

Zablokuj opencode czytanie plikأ³w `.env`:

```javascript title=".opencode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Wstrzyknij zmienne إ›rodowiskowe

Wstrzyknij zmienne konsekwencje dla wszystkich wykonaإ„ (narzؤ™dzia AI i terminale uإ¼ytkownika):

```javascript title=".opencode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Niestandardowe narzؤ™dzia

Wtyczki mogؤ… rأ³wnieإ¼ dodawaؤ‡ niestandardowe narzؤ™dzia do opencode:

```ts title=".opencode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@tera-system/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

Pomocnik `tool` tworzy narzؤ™dzie uإ¼ytkowe, ktأ³re moإ¼e wywoإ‚aؤ‡ kod opencode. Pobierane schematu Zoda i narzؤ™dzie wyszukiwania za pomocؤ…:

- `description`: Co robi do narzؤ™dzia
- `args`: Schemat Zoda dla narzؤ™dzi argumentأ³w
- `execute`: Funkcja uruchamiania po wywoإ‚aniu narzؤ™dzia

Twoje narzؤ™dzie bؤ™dzie dostؤ™pne dla otwartego kodu wraz z narzؤ™dziami uإ¼ytkowymi.

---

### Logowanie

uإ¼yj `client.app.log()` zamiast `console.log` do rejestracji strukturalnego:

```ts title=".opencode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Poziomy: `debug`, `info`, `warn`, `error`. Aby uzyskaؤ‡ szczegأ³إ‚owe informacje, zobacz [dokumentacjؤ™ pakietu SDK](https://opencode.ai/docs/sdk).

---

### Haki zagؤ™szczajؤ…ce

Dostosuj kontekst doإ‚ؤ…czony podczas kompaktowania sesji:

```ts title=".opencode/plugins/compaction.ts"
import type { Plugin } from "@tera-system/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

Hak `experimental.session.compacting` jest uruchamiany, zanim LLM wygeneruje podsumowanie kontynuacji. go, aby wstrzyknؤ…ؤ‡ kontekst kontekstu dla domeny, ktأ³rej nie dostrzeإ¼ono przez domyإ›lny monit o zagؤ™szczenie.

Moإ¼na takإ¼e umieإ›ciؤ‡ kompletny monit o zagؤ™szczenie, ustawiajؤ…c `output.prompt`:

```ts title=".opencode/plugins/custom-compaction.ts"
import type { Plugin } from "@tera-system/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

Gdy urzؤ…dzenie jest `output.prompt`, jest caإ‚kowicie dostؤ™pne na domyإ›lnym monit o zagؤ™szczanie. Tablica `output.context` jest w tym przypadku poniesiona.
