---
title: Egendefinerte verktأ¸y
description: Lag verktأ¸y som LLM kan kalle inn OpenCode.
---

Egendefinerte verktأ¸y er funksjoner du oppretter som LLM kan kalle under samtaler. De fungerer sammen med OpenCode sine [innebygde verktأ¸y](/docs/tools) som `read`, `write` og `bash`.

---

## Opprette et verktأ¸y

Verktأ¸y er definert som **TypeScript**- eller **JavaScript**-filer. Verktأ¸ydefinisjonen kan imidlertid pأ¥kalle skript skrevet pأ¥ **alle sprأ¥k** - TypeScript eller JavaScript brukes bare for selve verktأ¸ydefinisjonen.

---

### Plassering

De kan defineres:

- Lokalt ved أ¥ plassere dem i `.opencode/tools/`-katalogen til prosjektet ditt.
- Eller globalt, ved أ¥ plassere dem i `~/.config/opencode/tools/`.

---

### Struktur

Den enkleste mأ¥ten أ¥ lage verktأ¸y pأ¥ er أ¥ bruke `tool()`-hjelperen som gir typesikkerhet og validering.

```ts title=".opencode/tools/database.ts" {1}
import { tool } from "@tera-system/plugin"

export default tool({
  description: "Query the project database",
  args: {
    query: tool.schema.string().describe("SQL query to execute"),
  },
  async execute(args) {
    // Your database logic here
    return `Executed query: ${args.query}`
  },
})
```

**filnavnet** blir **verktأ¸ynavnet**. Ovennevnte oppretter et `database`-verktأ¸y.

---

#### Flere verktأ¸y per fil

Du kan ogsأ¥ eksportere flere verktأ¸y fra en enkelt fil. Hver eksport blir **et separat verktأ¸y** med navnet **`<filename>_<exportname>`**:

```ts title=".opencode/tools/math.ts"
import { tool } from "@tera-system/plugin"

export const add = tool({
  description: "Add two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiply two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

Dette lager to verktأ¸y: `math_add` og `math_multiply`.

---

#### Navnekollisjoner med innebygde verktأ¸y

Egendefinerte verktأ¸y er nأ¸klet etter verktأ¸ynavn. Hvis et egendefinert verktأ¸y bruker samme navn som et innebygd verktأ¸y, vil det egendefinerte verktأ¸yet ha forrang.

For eksempel erstatter denne filen det innebygde `bash`-verktأ¸yet:

```ts title=".opencode/tools/bash.ts"
import { tool } from "@tera-system/plugin"

export default tool({
  description: "Restricted bash wrapper",
  args: {
    command: tool.schema.string(),
  },
  async execute(args) {
    return `blocked: ${args.command}`
  },
})
```

:::note
Foretrekk unike navn med mindre du med vilje أ¸nsker أ¥ erstatte et innebygd verktأ¸y. Hvis du vil deaktivere et innebygd verktأ¸y, men ikke overstyre det, bruk [tillatelser](/docs/permissions).
:::

---

### Argumenter

Du kan bruke `tool.schema`, som bare er [Zod](https://zod.dev), for أ¥ definere argumenttyper.

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("SQL query to execute")
}
```

Du kan ogsأ¥ importere [Zod](https://zod.dev) direkte og returnere et vanlig objekt:

```ts {6}
import { z } from "zod"

export default {
  description: "Tool description",
  args: {
    param: z.string().describe("Parameter description"),
  },
  async execute(args, context) {
    // Tool implementation
    return "result"
  },
}
```

---

### Kontekst

Verktأ¸y mottar kontekst om gjeldende أ¸kt:

```ts title=".opencode/tools/project.ts" {8}
import { tool } from "@tera-system/plugin"

export default tool({
  description: "Get project information",
  args: {},
  async execute(args, context) {
    // Access context information
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

Bruk `context.directory` for أ¸ktens arbeidskatalog.
Bruk `context.worktree` for git-arbeidstreet.

---

## Eksempler

### Skrive et verktأ¸y i Python

Du kan skrive verktأ¸yene dine pأ¥ hvilket som helst sprأ¥k du vil. Her er et eksempel som legger til to tall ved hjelp av Python.

Fأ¸rst lager du verktأ¸yet som et Python-skript:

```python title=".opencode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

Lag deretter verktأ¸ydefinisjonen som pأ¥kaller den:

```ts title=".opencode/tools/python-add.ts" {10}
import { tool } from "@tera-system/plugin"
import path from "path"

export default tool({
  description: "Add two numbers using Python",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".opencode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

Her bruker vi [`Bun.$`](https://bun.com/docs/runtime/shell)-verktأ¸yet for أ¥ kjأ¸re Python-skriptet.
