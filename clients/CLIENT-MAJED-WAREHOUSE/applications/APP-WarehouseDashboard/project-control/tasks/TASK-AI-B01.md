# TASK-AI-B01 — Migration: Add AssistantEnabled + AssistantPrompt to DashboardCard

> **Status:** Draft → Approved  
> **Batch:** AI-B-1 (Backend Foundation — Parallel with B02, B03)  
> **Phase:** AI Assistant — Phase B (Backend Foundation)  
> **Plan Reference:** `project-preparation/AI_DASHBOARD_ASSISTANT_IMPLEMENTATION_PLAN.md` §6  

---

## 1. Objective

Add two new nullable fields to the `DashboardCard` entity, the corresponding DB migration, and the DbContext configuration:

| Field | Type | Default | Nullable | Purpose |
|---|---|---|---|---|
| `AssistantEnabled` | `bool` | `true` | No | Enables/disables the AI assistant for this card |
| `AssistantPrompt` | `string?` (nvarchar(max)) | `null` | Yes | Optional card-specific AI instructions |

---

## 2. Context

- **Technology Profile:** `dotnet-razorpages-adonet`
- **Project:** WarehouseDashboard  
- **ClientAppPath:** `D:\Teranoo Foundation\TeraSystem\TeraSystem-master\clients\CLIENT-MAJED-WAREHOUSE\applications\APP-WarehouseDashboard\`

---

## 3. Allowed Sources

Read these files for context (do not modify):

- `ClientAppPath\src\WarehouseDashboard.Web\Models\DashboardCard.cs` — existing entity
- `ClientAppPath\src\WarehouseDashboard.Web\Data\WarehouseDashboardDbContext.cs` — DbContext configuration
- `ClientAppPath\project-preparation\AI_DASHBOARD_ASSISTANT_IMPLEMENTATION_PLAN.md` — §6 Card Configuration Additions

---

## 4. Allowed Write Targets

Only these files may be created or modified:

1. `ClientAppPath\src\WarehouseDashboard.Web\Models\DashboardCard.cs` — add 2 properties
2. `ClientAppPath\src\WarehouseDashboard.Web\Data\WarehouseDashboardDbContext.cs` — add entity config (if needed)
3. A new EF Core migration file under `ClientAppPath\src\WarehouseDashboard.Web\Migrations\` — auto-generated by `dotnet ef migrations add AddAssistantFields`

---

## 5. Forbidden

- Must NOT change any existing property on DashboardCard.
- Must NOT change existing migrations.
- Must NOT change appsettings, Program.cs, or any other config file.
- Must NOT add any business logic — only model + migration.
- Must NOT delete or modify any other file.
- Must NOT change the namespace.

---

## 6. Technical Notes

- `AssistantEnabled` = `bool`, default `true`, non-nullable.
- `AssistantPrompt` = `string?`, default `null`, nullable, nvarchar(max).
- Add the properties IMMEDIATELY BEFORE `DashboardId` at line ~131 (keep grouping logical — all card settings together).
- After adding to the model, run: `dotnet ef migrations add AddAssistantFields --project src\WarehouseDashboard.Web`
- Verify the migration has only 2 new columns (no unintended schema changes).

---

## 7. Acceptance Criteria

| # | Criterion |
|---|---|
| AC-1 | `DashboardCard` model has `AssistantEnabled` property (bool, default true) |
| AC-2 | `DashboardCard` model has `AssistantPrompt` property (string?, nullable) |
| AC-3 | Migration file created successfully (0 errors) |
| AC-4 | `dotnet build` passes with 0 errors and 0 warnings |
| AC-5 | Existing `DashboardCard` properties remain unchanged |
| AC-6 | No other files modified outside Allowed Write Targets |

---

## 8. Pre-Execution Gate Result

```
Gate: PASS
Orchestration Level: Task Delegation Only
Model Tier: Light (model+field+migration, routine)
Security Sensitivity: Low (additive fields only)
Design Governance: N/A (no UI)
Technology Profile: dotnet-razorpages-adonet ✅
Client Approval Gate: N/A (internal client project, plan approved by Majed)
```

---

## 9. Handback Requirements

After completion, report:

1. Which files were created/modified.
2. The migration file name and its 2 column additions.
3. Build result (must be 0 errors, 0 warnings).
4. Any deviation or issue encountered.
