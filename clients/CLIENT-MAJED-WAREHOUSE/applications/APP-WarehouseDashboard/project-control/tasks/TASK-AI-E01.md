# TASK-AI-E01 — Migration: AssistantInsightLogs + AssistantUsageStats

> **Status:** Draft → Approved  
> **Batch:** E-1 (Sequential — then E02+E03 parallel)  
> **Phase:** AI Assistant — Phase E (Logs + Stats + Cache)  
> **Plan Reference:** `project-preparation/AI_DASHBOARD_ASSISTANT_IMPLEMENTATION_PLAN.md` §14  

---

## 1. Objective

Add two DB tables for assistant logging and usage statistics.

### AssistantInsightLogs

| Column | Type | Notes |
|---|---|---|
| Id | int (PK, identity) | Auto-increment |
| CardId | int (FK → DashboardCards) | Not null |
| Mode | nvarchar(20) | "explain" / "deep" |
| DepthLevel | int | 1-6 |
| RequestedAt | datetime2 | UTC |
| PromptVersion | nvarchar(20) | "1.0" |
| CardPromptUsed | bit | Was card-specific prompt used |
| DataScopeLabel | nvarchar(50) | "آخر 3 أشهر" etc |
| IsFullDataReached | bit | |
| WasCached | bit | |
| ResponseTimeMs | bigint | |
| ErrorCode | nvarchar(50) | null if success |

### AssistantUsageStats

| Column | Type | Notes |
|---|---|---|
| Id | int (PK, identity) | |
| CardId | int (FK → DashboardCards, unique) | One row per card |
| TotalRequests | int | Default 0 |
| ExplainRequests | int | Default 0 |
| DeepRequests | int | Default 0 |
| DeepenClicks | int | Default 0 |
| MostUsedDepth | int | Default 1 |
| LastUsedAt | datetime2 | null |
| AverageResponseTimeMs | bigint | Default 0 |
| CacheHitCount | int | Default 0 |
| CacheMissCount | int | Default 0 |

---

## 2. ClientAppPath

`D:\Teranoo Foundation\TeraSystem\TeraSystem-master\clients\CLIENT-MAJED-WAREHOUSE\applications\APP-WarehouseDashboard\`

---

## 3. Allowed Write Targets

1. `ClientAppPath\src\WarehouseDashboard.Web\Models\AssistantInsightLog.cs` — **NEW**
2. `ClientAppPath\src\WarehouseDashboard.Web\Models\AssistantUsageStat.cs` — **NEW**
3. `ClientAppPath\src\WarehouseDashboard.Web\Data\WarehouseDashboardDbContext.cs` — add DbSet + OnModelCreating config
4. Migration auto-generated by `dotnet ef migrations add AddAssistantLoggingTables`

---

## 4. Forbidden

- No business logic or services
- No changes to existing models
- No modification of appsettings or Program.cs

---

## 5. Acceptance

| # | Criterion |
|---|---|
| AC-1 | Two EF entity classes created |
| AC-2 | DbContext has DbSet for both tables |
| AC-3 | Migration created with both tables |
| AC-4 | CardId has FK → DashboardCards.Id with cascade delete |
| AC-5 | `dotnet build` passes with 0 errors, 0 warnings |

---

## 6. Pre-Execution Gate

```
Gate: PASS | Security: Low | Model: Light | Technology Profile: ✅
```
